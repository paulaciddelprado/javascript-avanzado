const numero1 = parseInt(prompt("Escribe un primer número: "));
const numero2 = parseInt(prompt("Escribe un segundo número: "));

if (numero1 % 2 !== 0)
    (numero2 % 2 !== 0)
    {
    document.write("Los números impares que existen entre " + numero1 + " y " + numero2 + " son: ");
}

else {
    document.write("Número no detectado");
}
