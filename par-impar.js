const numero = parseInt(prompt("Escribe un número: "));
if (numero % 2 == 0) {
    document.write(numero + ", la cantidad ingresada es par.");
}

else {
    document.write(numero + ", la cantidad ingresada es par.");
}

