var numero = parseInt(prompt("Escribe un número: "));
var suma = 0;
var plus;
var n = 1;
var contador = array ();

while (numero > 0) {
    plus = parseInt(prompt("Escribe un número: "));
    if (plus > 0)
    suma= numero + plus;
    
    else
        break;
    n++;
    contador = array; 
}
document.write("Suma: ", suma); 
document.write ("Media: ", suma/n);
document.write ("Números seleccionados: ", contador);